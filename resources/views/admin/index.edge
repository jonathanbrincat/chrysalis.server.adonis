@layout('_layout/master')

@section('content')
  <div class="container">
    @if(flashMessage('info'))
    <div class="row">
      <div class="col-12">
        <p class="alert alert-info">{{ flashMessage('info') }}</p>
      </div>
    </div>
    @endif

    <div class="row">
      <div class="col-12">
        <header class='ui__toolbar'>
          <p class="ui__field">
            <a class="ui__btn btn__primary" href="{{ route('admin.create') }}" >New Post</a>
          </p>
        </header>

        <h2>My Posts</h2>
        @each(post in userPosts)
        <article class="admin__post">
          <p>{{ post.title }}</p>
          <p>{{ post.content }}</p>

          <div class="ui__controlbar">
            <p class="ui__field">
              <a class="ui__btn" href="{{ route('admin.edit', { id: post.id}) }}">Edit</a>
            </p>
          </div>
        </article>
        @endeach

        <h2>All Posts</h2>

        @each(post in posts)
        <article class="admin__post">
          <p>{{ post.title }}</p>

          <div class="ui__controlbar">
            <p class="ui__field">
              <a class="ui__btn" href="{{ route('admin.edit', { id: post.id }) }}">Edit</a>
            </p>
            <p class="ui__field">
              <a class="ui__btn" href="{{ route('admin.delete', { id: post.id }) }}">Delete</a>
              {{-- <a href="{{ route('PostController.getAdminDelete', { id: post.id }) }}" class="ui__btn">Delete</a> --}}
            </p>
          </div>
        </article>
        @endeach
      </div>
    </div>
  </div>
@endsection
