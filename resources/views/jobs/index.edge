@layout('_layout/master')

@section('content')
<h1>All Jobs</h1>

<ul class="list-group">
  @each(job in jobs)
  <li class="list-group-item">
    <p><a href="{{ job.link }}">{{ job.title }}</a></p>
    <p>{{ job.description }}</p>
  </li>
  @endeach
</ul>
@endsection
